<!--
Paste this block into each repo's CLAUDE.md (replacing any duplicated
conventions/lessons prose). The @import lines pull in the synced shared standards
so there is one source of truth. Keep only repo-SPECIFIC guidance in CLAUDE.md.
-->

## Shared standards (single source of truth)

These are synced from [`flowolf86/.github`](https://github.com/flowolf86/.github)
into `.standards/` by the `sync-standards` workflow. **Do not edit the copies** —
change the source in that repo and let the bot PR the update.

@.standards/CONVENTIONS.md
@.standards/LESSONS.md
